---
layout: null
permalink: /feed.xml
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <id>{{ "/" | absolute_url }}</id>
  <title>{{ site.title | xml_escape }}</title>
  <subtitle>{{ site.description | xml_escape }}</subtitle>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author>
    <name>{{ site.social.name | xml_escape }}</name>
    <uri>{{ "/" | absolute_url }}</uri>
  </author>
  <link rel="self" type="application/atom+xml" href="{{ page.url | absolute_url }}"/>
  <link rel="alternate" type="text/html" hreflang="{{ site.lang }}" href="{{ "/" | absolute_url }}"/>
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <rights>© {{ "now" | date: "%Y" }} {{ site.social.name | xml_escape }}</rights>
  <icon>{{ "/assets/img/favicons/favicon.ico" | absolute_url }}</icon>
  {% for post in site.posts limit: 20 %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.url | absolute_url }}</id>
    <author><name>{{ post.author | default: site.social.name | xml_escape }}</name></author>
    {% for category in post.categories %}
    <category term="{{ category | xml_escape }}"/>
    {% endfor %}
    <summary type="text">{{ post.excerpt | strip_html | strip_newlines | truncate: 400 | xml_escape }}</summary>
  </entry>
  {% endfor %}
</feed>

